version: v1
# Application name (must match nova-build.yaml)
name: 112-new-chat-bot

# Target image (updated by build workflow in app-hub)
target: nova-apps/112-new-chat-bot:1.0.6

sources:
  # Application image (updated by build workflow in app-hub)
  app: 112-new-chat-bot:1.0.6

defaults:
  cpu_count: 2
  memory_mb: 2048
ingress:
  - listen_port: 8000
  - listen_port: 18001
egress:
  allow:
    - "**"
    - "0.0.0.0/0"
    - "::/0"
api:
  listen_port: 18000
aux_api:
  listen_port: 18001
# S3 Storage Configuration
storage:
  s3:
    enabled: true
    bucket: "nova-app-storage-prod"
    prefix: "apps/112/"
    region: "us-west-1"
