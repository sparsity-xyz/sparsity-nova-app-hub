name: "f27mas-test-fs-app"
version: "1.0.0"
repo: "https://github.com/sparsity-xyz/sparsity-nova-examples"
ref: "main"
build:
  directory: "hello-world-tee"
  dockerfile: "Dockerfile"

metadata:
  description: "test filesystem"
