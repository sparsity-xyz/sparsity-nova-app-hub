version: v1
# Application name (must match nova-build.yaml)
name: 110-secure-chat-bot

# Target image (updated by build workflow in app-hub)
target: nova-apps/110-secure-chat-bot:1.0.0

sources:
  # Application image (updated by build workflow in app-hub)
  app: 110-secure-chat-bot:1.0.0

defaults:
  cpu_count: 2
  memory_mb: 2048
ingress:
  - listen_port: 8000
  - listen_port: 18001
egress:
  allow:
    - "**"
api:
  listen_port: 18000
aux_api:
  listen_port: 18001

